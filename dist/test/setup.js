"use strict";
// 在所有测试之前运行
beforeAll(() => {
    // 这里可以添加全局的测试设置
    // 例如：连接测试数据库、设置全局变量等
});
// 在所有测试之后运行
afterAll(() => {
    // 这里可以添加全局的清理代码
    // 例如：断开数据库连接、清理临时文件等
});
// 在每个测试之前运行
beforeEach(() => {
    // 这里可以添加每个测试前的设置
    // 例如：重置数据库状态、清除缓存等
});
// 在每个测试之后运行
afterEach(() => {
    // 这里可以添加每个测试后的清理代码
    // 例如：清理测试数据、重置模拟等
    jest.clearAllMocks();
});
